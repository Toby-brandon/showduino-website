# Showduino Website

This repository contains the Showduino project. The marketing website for Showduino lives in the `/website` folder.

Website commands:

  cd website
  npm install
  npm run dev
  npm run build

The website is built with Vite + React + TypeScript + TailwindCSS and is configured for Firebase Hosting.
